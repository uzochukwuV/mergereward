# ⚠️  NEVER commit this file. Add to .gitignore.
# These secrets are encrypted and stored inside the CRE TEE.
# They are injected at runtime and never appear on-chain.

# GitHub Personal Access Token (repo read scope is sufficient)
GITHUB_TOKEN=ghp_REPLACE_ME

# (Optional) Notification webhook — e.g. Slack or your backend
NOTIFY_WEBHOOK_URL=https://hooks.slack.com/REPLACE_ME
